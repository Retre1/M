# Рабочий журнал

Назначение — не пересказ коммитов (для этого есть `git log`), а **память о
граблях**: что уже проверено, какие решения приняты и почему, на чём я
ошибался. Читать перед тем, как менять обучение, метрики или CI.

Дописывать сверху вниз по датам. Не переписывать прошлые записи — если вывод
оказался неверным, добавить новую запись с опровержением.

---

## Правила работы в этом репозитории

1. **Менять только то, что требуется задачей.** Широкие правки (форматирование,
   массовые переименования) делать отдельным коммитом и только по явной
   договорённости — иначе содержательное изменение тонет в диффе.
2. **Проверять утверждение по коду, а не по документации.** В этом репозитории
   документация неоднократно расходилась с кодом (см. ловушку №1).
3. **После каждой правки — `pytest tests/unit/` и `ruff check src/ tests/`.**
   Обе команды локально зелёные до коммита.
4. **Тест, который не может упасть, — не тест.** Проверять ценность теста
   поломкой того, что он охраняет.

---

## Ловушки репозитория

Проверенные факты. Если что-то из этого снова «не работает» — сначала сюда.

| # | Ловушка | Признак |
|---|---|---|
| 1 | Документация отставала от кода | `ProfitFocusedReward` числился действующим, хотя жил только в архиве |
| 2 | Совпадение имени файла и каталога тестов ломает сбор | `tests/unit/test_backtest.py` + `tests/unit/test_backtest/` → `Interrupted: 1 error during collection` |
| 3 | Тесты зависели от дня недели | `WeekendGapGuard` и `LiquidityGuard` читают `datetime.now(UTC)`; по выходным падало 12 тестов. Лечится фикстурой `frozen_trading_clock` |
| 4 | Ключ метрики — `total_trades`, не `n_trades` | Везде тихо читались нули |
| 5 | TruffleHog: детектор Lob ловит имена pytest-методов | Формат ключа Lob = `[a-z]{4}_` + 35 символов = ровно 40. В репозитории 16 таких имён. Детектор исключён в CI |
| 6 | Индекс PyTorch заблокирован прокси | `download.pytorch.org` → 403. Ставить torch с обычного PyPI |
| 7 | Аннуализация была прибита к 252 | На H1 ошибка ×24 по горизонту и ×√24 по волатильности |

---

## 2026-08-09 — Фаза 2 (частично): baseline как ворота

**Сделано.** `backtest/baselines.py` (buy & hold, MA cross, Donchian, random) и
`backtest/comparison.py` — прогон кандидата и baseline через один движок.

**Найдено по ходу, исправлено:**
- Аннуализация на жёстких 252 → период выводится из меток кривой капитала.
- Sharpe взрывался на плоских стратегиях: `(0 − 0.05)/~0` = −343 при годовой
  волатильности 0.015 %. Ниже порога возвращается 0.0.
- Сравнение читало несуществующий ключ `n_trades`.

**Найдено, НЕ исправлено — требует решения владельца.** Сайзер выдаёт
ничтожную экспозицию, и это не баг агента:

```
max_value = 100 000 × 0.10             = 10 000
raw_value = confidence × 0.25 × 10 000 = 2 500 × confidence
lots      = 2 500 / (1.08 × 100 000)   = 0.023  при confidence = 1.0
```

Максимум — 0.023 лота; при `|action| < 0.43` округление даёт ноль. Замер:
**671 отказ из 1155** с причиной «Position size computed to zero», прошедшие
сделки — минимальным лотом. Это форма Run 5 ($50 нотионала на $100k).

`max_position_pct` и дробь Келли — риск-политика живой торговли. Вместо
самовольной правки состояние сделано видимым: `avg_exposure_pct` в выводе и
отказ считать вердикт доказательством при ничтожной экспозиции.

**Открыто:** решение по сайзингу (поднять потолок / перейти на ATR-сайзинг от
риска на сделку / оставить); реалистичные издержки (спред 1.0 → 2.0–2.5 пипса).

**Моя ошибка:** создал `tests/unit/test_backtest/` рядом с существующим
`test_backtest.py` — сбор тестов сломался. См. ловушку №2.

---

## 2026-08-09 — Фаза 0: метрики обучения на реализованном PnL

**Причина.** `sharpe` считался как `mean/std` по ряду наград, `profit_factor` —
суммой положительных наград против отрицательных. Все прогоны имели
отрицательный `ep_rew_mean` → множество положительных наград пусто → `PF ≡ 0`
независимо от торговли. Отсюда же «Sharpe −700000».

**Сделано.** `ForexTradingEnv` отдаёт `info["episode_financials"]` на
терминальном шаге; `CurriculumV2Callback` считает через `utils/metrics.py`.
Ключи `sharpe` и `profit_factor` сохранены — отбор чекпоинтов и early-stop
перешли на финансовые величины без правок на своей стороне. Старая величина —
`reward_sharpe`.

**Побочно:** `min_delta_profit_factor = -1.0` (заглушка Run 4) означала, что
early-stop **не мог сработать никогда**; аннуализация `sqrt(252)` на H1;
`sortino_ratio` возвращал NaN при одном отрицательном наблюдении.

**Мои ошибки:** два написанных теста были неверны — один обращался к
`self.model` у неприсоединённого колбэка, второй подавал постоянный ряд
(нулевая дисперсия → Sharpe не определён).

---

## 2026-08-08/09 — Фаза 1: слияние веток и починка CI

**Причина.** Прогоны 1–6 шли на `v2.0-quantum-hybrid`, фиксы аудита — на
`main`; ни одна ветка не содержала обе половины.

**Сделано.** Мерж (5 конфликтов). Ключевое решение: сохранены `_soft_saturate`
(tanh-фикс аудита) и `OnlineSharpeTracker` из `main`, восстановлен
`ProfitFocusedReward` из v2. За `main`-тренером оставлен `TradingReward`, выбор
v2 не тронут внутри `trainer_v2` — **это решение стоит пересмотреть после
починки метрик**. Снимок `src/_v2_dump/` удалён.

**CI.** `pytest-timeout` отсутствовал в `[dev]` → pytest выходил с кодом 4 до
сбора тестов, **ни один тест не выполнялся с марта**. Следом вскрылись
незадекларированный `sb3-contrib` и 12 тестов, зависящих от дня недели.
`ruff` 462 → 0.

**Моя ошибка:** при разрешении конфликта оставил `TradingReward`, но блок
импортов приехал со стороны v2, где импортируется `ProfitFocusedReward`.
Тесты это не поймали — ни один не строит тренировочное окружение. Поймал
`F821`. Вывод: линтер здесь не косметика.

**Слишком широко:** `ruff format` затронул 37 файлов ради 105 длинных строк.
Содержательные правки в том же диапазоне коммитов читать труднее. Впредь —
отдельным коммитом и по договорённости.

---

## 2026-08-08 — Анализ: метрики прогонов 1–6 недействительны

Разбор в [[analysis/metrics-invalidation|отдельном документе]]. Кратко: ни одно
число из хронологии прогонов 1–6 не характеризует торговлю; сопоставим только
OOS-бэктест, и он ухудшился (PF 0.957 → 0.736).

**Моя ошибка:** в первой версии ТЗ назвал модуль вознаграждения
`ProfitFocusedReward`, взяв имя из существующей документации и не проверив по
коду. Класса в активном коде тогда не было. См. правило №2.

---

#worklog #process
